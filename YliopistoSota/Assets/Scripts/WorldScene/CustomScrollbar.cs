using UnityEngine;
using System.Collections;
using UnityEngine.UI;

public class CustomScrollbar : MonoBehaviour {

    

	// Use this for initialization
	void Start () {
	   gameObject.GetComponent<Scrollbar>().size = 0.32f;
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
